# AdjudicArts

Webhook trigger test: 2026-02-25
# Deployment reminder

- Aggregated applicant feedback uses AI quality output only when `OPENAI_API_KEY` is set in Vercel environment variables.
- After adding/updating `OPENAI_API_KEY`, redeploy the project to apply it.
